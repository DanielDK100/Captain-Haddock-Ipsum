<!DOCTYPE html>
<html lang="da">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" sizes="32x32" href="images/terry.webp">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Bangers&family=Comic+Neue:ital,wght@1,700&family=Lobster&display=swap"
    rel="stylesheet">
  <style>
    * {
      padding: 0;
      margin: 0;
    }

    body {
      background: #ffecc5;
    }

    .speech-bubble {
      position: absolute;
      height: 70px;
      width: 100%;
      background: url("images/speech-bubble.webp") no-repeat;
      background-size: contain;
      right: -245px;
      top: -100px;
      padding: 50px 50px;
      font-family: 'Bangers', cursive;
      font-size: 2.5rem;
      color: #343434;
      letter-spacing: 1px;
      text-align: center;
      display: flex;
      justify-content: center;
      align-items: center;

    }


    .flex-box {
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      margin-top: 8rem;
    }

    .container {
      max-width: 80rem;
      padding: 2rem;
    }

    .haddock {
      position: relative;
    }

    .header {
      font-family: 'Bangers', cursive;
      font-size: 5rem;
      letter-spacing: 4px;
      color: #343434;
    }

    .paragraph {
      font-family: 'Lobster', cursive;
      font-size: 2.5rem;
      color: #505050;

    }

    .lorem {
      font-size: 1.5rem;
      font-family: 'Comic Neue', cursive;
      color: #3e3e3e;
      line-height: 35px;
      margin-bottom: 1.5rem;
    }

    .generate-paragraph-form input[type=number] {
      padding: 1rem;
      outline: none;
      width: 100%;
      margin-top: 1.5rem;
      font-family: 'Lobster', cursive;
      border: 4px solid white;
      background-color: #f1f1f1;
      color: #343434;
      font-size: 1.5rem;
    }

    .generate-paragraph-form input[type=submit] {
      padding: 1rem 2rem;
      outline: none;
      margin-top: 1rem;
      margin-bottom: 2rem;
      font-family: 'Lobster', cursive;
      font-size: 1.5rem;
      border: 4px solid white;
      background-color: #57a2cd;
      color: white;
      cursor: pointer;
    }

    .generate-paragraph-form span {
      padding: 1rem 2rem;
      outline: none;
      margin-top: 1rem;
      margin-bottom: 2rem;
      font-family: 'Lobster', cursive;
      font-size: 1.5rem;
      border: 4px solid white;
      background-color: #c44928;
      color: white;
      cursor: pointer;
    }

    .snowy {
      height: 10%;
      position: fixed;
      bottom: 0px;
      right: 0;
    }
  </style>
  <title>
    Kaptajn Haddock Ipsum <%= insult ? ' | ' + insult + '!' : '' %>
  </title>
</head>

<body>
  <div class="flex-box">
    <a class="haddock" href="/">
      <% if (insult) { %>
        <div class="speech-bubble">
          <%= insult %>!
        </div>
        <% } %>
          <img src="images/captajn-haddock.webp" alt="Kaptajn Haddock">
    </a>
    <div class="container">
      <h1 class="header">Kaptajn Haddock Ipsum</h1>
      <p class="paragraph">
        Genererer Lorem Ipsum tekst med et drys af Kaptajn Haddocks farverige skældsord! Indtast nedenfor
        antallet af paragraffer der skal genereres.
      </p>

      <form class="generate-paragraph-form" action="/generate-paragraphs" method="post">
        <input type="number" name="numberOfParagraphs" min="1" max="1000" placeholder="Antal paragraffer">
        <input type="submit" value="Generer tekst!">
        <% if (paragraphs.length !=0) { %>
          <span onclick="copyToClipboard('paragraphs')" class="copy">Kopier!</span>
          <% } %>
      </form>

      <% if (paragraphs.length !=0) { %>
        <div>
          <div id="paragraphs">
            <% paragraphs.forEach(function(paragraph){ %>
              <p class="lorem">
                <%= paragraph %>
              </p>
              <% }); %>
          </div>
        </div>
        <% } %>
    </div>
  </div>
  <a href="/generate-paragraphs-json?numberOfParagraphs=7" target="_blank" title="API endpoint">
    <img src="images/snowy.webp" alt="Snowy" class="snowy">
  </a>
  <script>
    function copyToClipboard(id) {
      const range = document.createRange();
      range.selectNode(document.getElementById(id));
      window.getSelection().removeAllRanges();
      window.getSelection().addRange(range);
      try {
        document.execCommand('copy');
        window.getSelection().removeAllRanges();
        document.querySelector('.copy').style.display = 'none';
      } catch (err) {
        console.log('Unable to copy!');
      }
    }
  </script>
</body>

</html>